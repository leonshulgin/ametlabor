var links = ["link1", "link2", "link3"]; // Example array of links
var iterations = 5; // Number of iterations you want to run the loop

for (var k = 0, n = links.length; k < iterations; ++k) {
    console.log("Iteration:", k);
    console.log("Number of links:", n);
    // You can add more code here to perform operations during each iteration
}
